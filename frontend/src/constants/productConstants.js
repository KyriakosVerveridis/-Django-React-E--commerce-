// --- Product Action Constants ---

// Sent when the request starts (to trigger a loading spinner)
export const PRODUCT_LIST_REQUEST = 'PRODUCT_LIST_REQUEST'

// Sent when data is successfully received from the Django API
export const PRODUCT_LIST_SUCCESS = 'PRODUCT_LIST_SUCCESS'

// Sent if there is an error during the API call (network or server error)
export const PRODUCT_LIST_FAIL = 'PRODUCT_LIST_FAIL'